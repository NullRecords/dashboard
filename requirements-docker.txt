# Lean Docker requirements - only what's needed for web dashboard
# ~40% smaller than full requirements.txt

# Core FastAPI and web server
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
jinja2==3.1.2

# HTTP clients (only need one async client)
httpx==0.25.2
aiohttp==3.9.1

# Google API dependencies
google-api-python-client==2.108.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Data validation and settings
pydantic==2.5.0
pydantic-settings==2.1.0
PyYAML==6.0.1
python-dotenv==1.0.0

# Date and time handling
python-dateutil==2.8.2
pytz==2023.3

# Calendar parsing
icalendar>=5.0.0

# Web scraping and parsing
beautifulsoup4==4.13.5
lxml==4.9.3
feedparser==6.0.10

# Email security (FounderShield)
dnspython>=2.4.0
python-whois>=0.8.0

# AI integration
openai==1.3.0

# System monitoring
psutil==5.9.6

# YouTube search (for music widget)
youtube-search-python>=1.6.6

# REMOVED for Docker (not needed):
# - pandas, numpy, matplotlib, seaborn, plotly (data viz - not used)
# - pywebview (desktop app only)
# - requests (using httpx instead)
# - schedule (cron handles scheduling in Docker)
# - pytest, pytest-asyncio (testing - not needed in production)
